{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/test/mysoc",
            "args": ["-h"],
            "cwd": "${workspaceFolder}/test"
        },
    ],
    "version": "2.0.0"
}